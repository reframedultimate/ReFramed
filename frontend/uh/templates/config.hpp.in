#pragma once

#cmakedefine UH_REFCOUNTED_DEBUG

#define STRINGIFY_(x) #x
#define STRINGIFY(x) STRINGIFY_(x)

#define UH_VERSION_MAJOR ${PROJECT_VERSION_MAJOR}
#define UH_VERSION_MINOR ${PROJECT_VERSION_MINOR}
#define UH_VERSION_PATCH ${PROJECT_VERSION_PATCH}
#define UH_VERSION \
    ((UH_VERSION_MAJOR << 16) | (UH_VERSION_MINOR << 8) | UH_VERSION_PATCH)
#define UH_VERSION_STR \
    STRINGIFY(UH_VERSION_MAJOR) "." STRINGIFY(UH_VERSION_MINOR) "." STRINGIFY(UH_VERSION_PATCH)

#if defined(UH_BUILDING)
#   define UH_PUBLIC_API ${UH_API_EXPORT}
#else
#   define UH_PUBLIC_API ${UH_API_IMPORT}
#endif
#define UH_PRIVATE_API ${UH_API_LOCAL}
